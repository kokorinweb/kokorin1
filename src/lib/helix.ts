/**
 * Сцена скролл-скраба: секвенция кадров из исходного ролика + сценарий надписей.
 *
 * Кадры лежат в public/helix и нарезаны так, чтобы 1:1 совпадать с этим конфигом.
 * Меняешь исходник — перегенерируй кадры и поправь frameCount, больше нигде трогать
 * ничего не нужно: страница читает длину сцены только отсюда.
 */

export const HELIX = {
  /**
   * Кадров вдвое больше, чем в исходнике: последовательность прогнали через motion
   * interpolation до 48 к/с, иначе на прокрутке видно ступеньки. И апскейл ×2 lanczos —
   * браузер растягивал 568 пикселей на весь экран билинейно, отсюда и было мыло.
   */
  frameCount: 341,
  /** Реальный размер кадра. Нужен канвасу, чтобы считать вписывание без загрузки картинки. */
  width: 1136,
  height: 720,
  /** Сколько экранов прокрутки занимает сцена. Меньше — быстрее, но дёрганее. */
  runwayScreens: 6.2,
  src: (index: number) => `/helix/f_${String(index + 1).padStart(3, "0")}.webp`,
};

export type HelixCaption = {
  /** Начало и конец окна в долях прогресса сцены (0…1). */
  from: number;
  to: number;
  place: "center" | "left" | "right";
  kicker: string;
  title: string;
  body: string;
};

/**
 * Окна подобраны под монтаж ролика: крупный план разрыва → отъезд на всю спираль →
 * пролёт вдоль молекулы → нырок обратно к перекладинам → финал.
 */
export const HELIX_CAPTIONS: HelixCaption[] = [
  {
    // Уходит в минус нарочно: на самом верху сцены надпись уже должна быть читаемой.
    from: -0.08,
    to: 0.2,
    place: "center",
    kicker: "Геномная диагностика",
    title: "Одна ошибка на три миллиарда",
    body: "Столько пар оснований в вашем геноме. Чтобы организм сменил правила игры, достаточно сломаться одной.",
  },
  {
    from: 0.24,
    to: 0.43,
    place: "left",
    kicker: "01 — Чтение",
    title: "Мы видим всю молекулу",
    body: "Полногеномное секвенирование с покрытием 30×. От пробирки до готового отчёта — шесть часов, а не три недели.",
  },
  {
    from: 0.47,
    to: 0.64,
    place: "right",
    kicker: "02 — Интерпретация",
    title: "14 000 значимых маркеров",
    body: "Каждый вариант сверяется с клиническими когортами и базами ClinVar, а не с обещаниями из рекламы.",
  },
  {
    from: 0.68,
    to: 0.85,
    place: "left",
    kicker: "03 — Прогноз",
    title: "Раньше, чем станет диагнозом",
    body: "Онкориски, фармакогенетика, наследственные синдромы. Мы находим их до того, как появится первый симптом.",
  },
  {
    // Хвост за единицу: финальная карточка не должна гаснуть на последнем кадре.
    from: 0.88,
    to: 1.2,
    place: "center",
    kicker: "",
    title: "NUCLEA",
    body: "Прочитать геном — один раз. Пользоваться результатом — всю жизнь.",
  },
];
